export enum Bedtypes {
    normal,
    imc,
    icu,
    covid,
    covidIcu
}

export enum Wardtypes {
    normal,
    imc,
    icu,
    covid,
}

export enum Bedstates {
    occupied,
    free,
    maybe,
    out_of_order,
    unknown
}